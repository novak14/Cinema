@model PlaceViewModel
<h1>Hello Bitches</h1>


<form asp-controller="Order" asp-action="Order" method="post">

    @{
        for (int i = 0; i < Model.plac.Count(); i++)
        {
            if (Model.plac[i] != null)
            {

            <div class="checkbox">
                <label>
                    <input asp-for="@Model.plac[i].checkboxAnswer" type="checkbox" />
                    @Model.plac[i].IdNumberPlace
                    <input asp-for="@Model.plac[i].IdPlace" type="hidden" />
                    <input asp-for="@Model.plac[i].IdPlace" type="hidden" />
                </label>
                <br />
            </div>
                }
            }
        }

    @*@{
        foreach (var item in Model)
        {
            <div class="checkbox">
                <label>
                    <input asp-for="@item.checkboxAnswer" type="checkbox" />
                    @item.IdNumberPlace
                    <input asp-for="@item.IdPlace" type="hidden" />
                    <input asp-for="@item.IdPlace" type="hidden" />
                </label>
                <br />
            </div>
        }
    }*@
    <input type="hidden" asp-for="@Model.IdFilm" />
    <input type="hidden" asp-for="@Model.Date" />


    <button type="submit" class="btn btn-warning" value="Submit">Filter</button>
    </form>
